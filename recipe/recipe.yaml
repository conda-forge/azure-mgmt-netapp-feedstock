schema_version: 1

context:
  name: azure-mgmt-netapp
  version: "17.0.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/azure_mgmt_netapp-${{ version }}.tar.gz
  sha256: 66cd6404a280c8cb3bf1c1f83684b886e3d08c4ce4a13c3f3bb6d3eff38d1fd0

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - setuptools
    - python ${{ python_min }}.*
    - pip
  run:
    - python >=${{ python_min }}
    - isodate >=0.6.1
    - typing_extensions >=4.6.0
    - azure-mgmt-core >=1.6.0
tests:
  - python:
      imports:
        - azure.mgmt.netapp
        - azure.mgmt.netapp.aio
      pip_check: true
      python_version: ${{ python_min }}.*
about:
  summary: Microsoft Azure NetApp Files Management Client Library for Python
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/Azure/azure-sdk-for-python

extra:
  recipe-maintainers:
    - janjagusch
